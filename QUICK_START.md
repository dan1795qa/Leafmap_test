# 🚀 Быстрый старт Leafmap

## Пошаговая инструкция для начинающих

### Шаг 1️⃣ - Открытие проекта

1. Клонируйте репозиторий:
```bash
git clone https://github.com/dan1795qa/Leafmap_test.git
cd Leafmap_test
```

### Шаг 2️⃣ - Запуск локального сервера

#### Способ 1: Использование Python (рекомендуется)

Если у вас установлен Python:

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

Откройте браузер: **http://localhost:8000**

#### Способ 2: Использование Node.js

Если у вас установлен Node.js:

```bash
# Сначала установите зависимости
npm install

# Затем запустите
npm start
```

#### Способ 3: Прямой запуск файла

Просто дважды нажмите на `index.html` в вашей папке.

### Шаг 3️⃣ - Проверка работы

Вы должны увидеть:
- ✅ Зеленую шапку приложения с названием "Leafmap"
- ✅ Боковую панель слева с управлением
- ✅ Интерактивную карту в центре
- ✅ 5 маркеров на карте (ориентиры в Минске)

### Шаг 4️⃣ - Первые тесты

1. **Попробуйте поиск:**
   - Введите "площ" в поле поиска
   - На карте должна выделиться Центральная площадь

2. **Тестируйте масштаб:**
   - Осэ слайдер "Масштаб"
   - Ор прокручивайте колесо мыши на карте

3. **Кликайте на маркеры:**
   - Каждый маркер показывает информацию в панели справа

4. **Проверьте консоль:**
   - Нажмите F12 → вкладка Console
   - Там должны быть логи инициализации

## 📑 Первые модификации

### Добавить новую локацию

1. Откройте `config.js`
2. Найдите массив `locations`
3. Добавьте новый объект:

```javascript
{
    id: 6,
    name: 'Моя локация',
    lat: 53.9000,
    lng: 27.5500,
    description: 'Описание моего места',
    icon: 'marker',
    type: 'landmark'
}
```

4. Сохраните файл и обновите браузер (F5)

### Изменить центр карты

В `config.js` найдите строку:
```javascript
center: [53.9045, 27.5615],  // Это координаты Минска
```

Замените на ваши координаты:
```javascript
center: [55.7558, 37.6173],  // Пример: Москва
```

### Изменить цвета

В `styles.css` найдите переменные:
```css
:root {
    --primary-color: #2ecc71;      /* Зелёный */
    --secondary-color: #3498db;    /* Голубой */
}
```

### Добавить новый слой маркеров

1. В `config.js` добавьте новый тип локации в `markerStyles`
2. В `locations` используйте этот новый тип
3. Результат обновится автоматически

## 🞭 Часто задаваемые вопросы

**Q: Карта не загружается**
A: Проверьте интернет-соединение. Карты загружаются из OpenStreetMap.

**Q: Маркеры не видны**
A: Убедитесь, что чекбокс "Маркеры" включен в левой панели.

**Q: Где найти помощь?**
A: Смотрите `README.md` для подробной документации.

## ✅ Чек-лист для начинающих

- [ ] Клонировал репозиторий
- [ ] Запустил локальный сервер
- [ ] Вижу карту в браузере
- [ ] Попробовал поиск
- [ ] Кликнул на маркеры
- [ ] Открыл консоль (F12)
- [ ] Добавил новую локацию
- [ ] Изменил центр карты
- [ ] Изменил цвета
- [ ] Прочитал README.md

---

**Поздравляем! 🎉 Вы готовы работать с Leafmap!**
